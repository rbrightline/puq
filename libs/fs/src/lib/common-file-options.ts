export type CommonFileOptions = {
  /**
   * Determine the operation will recursively run for the sub directories.
   */
  recursive?: boolean;

  /**
   * Determine the output path/paths will be absolute or relative
   */
  fullpath?: boolean;
};
