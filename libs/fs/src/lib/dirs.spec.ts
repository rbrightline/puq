describe('dirs: list all dirs', () => {
  beforeAll(async () => {
    // Setup
  });

  afterAll(async () => {
    // Teardonw
  });

  it('should pass', async () => {
    // Test
    expect(1).toEqual(1);
  });
});
